cpp: |
  #include <bits/stdc++.h>
  using namespace std;
  
  int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    bool first = true;
    while (getline(cin, s)) {
      if (!first) cout << '\n';
      first = false;
      cout << s;
    }
    return 0;
  }
python: |
  print(input())