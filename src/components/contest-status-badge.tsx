import { Badge } from "./ui/badge";

export function ContestStatusBadge(props: { startTime: Date; endTime: Date }) {
	// TODO: implement dynamic badge based on start and end time
	return <Badge>starts in 2 min</Badge>;
}
