services:
  localbox:
    image: ghcr.io/thecomputerm/localbox:latest
    cgroup: host
    privileged: true
    ports:
      - "2000:2000"
    tmpfs:
      - /tmp:exec

  db:
    image: ghcr.io/pgmq/pg18-pgmq:v1.7.0
    env_file: .env
    ports:
      - "5432:5432"